# ---- 编译产物 ----
# 只忽略仓库根目录下的 migrate 二进制，不能写成裸 migrate，
# 否则会连带忽略 cmd/migrate/ 源码目录。
/migrate
*.exe
*.test
*.out

# ---- 含真实凭据的配置（只提交 *.example.json 模板）----
mongodb_full_migration_config.json
# 如果你在 sample_config.json / mongodb_replication_config.json 里填了真实连接串，
# 建议改用自己的私有文件名并在此忽略，例如：
# my_config.json

# ---- 迁移运行时状态（断点续传 / 检查点，勿提交）----
resumeToken-*.json
resumeToken-*.json.*
oplogTimestamp-*.json
oplogTimestamp-*.json.backup
initialMigrationState-*.json
id-mapping.jsonl

# ---- 死信队列 / 评估修复审计 / 修复计划 ----
dlq-*.jsonl
remediation-audit.jsonl
remediation-plan.json
remediation-plan.json.bak-*

# ---- 日志 / 进程文件 ----
*.log
console-*.log
*.pid

# ---- 系统 / 编辑器杂项 ----
.DS_Store
Thumbs.db
*.swp
*~
.idea/
.vscode/
