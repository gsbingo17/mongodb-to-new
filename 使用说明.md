# MongoDB → Firestore 迁移工具 · 使用说明

本工具在原 MongoDB→MongoDB 复制工具基础上做了增强，重点支持 **MongoDB → Firestore（MongoDB 兼容模式）** 的全量迁移 + 实时增量复制，并新增了：

- 🌐 **浏览器控制台（console 模式）** —— 在网页里配置、评估、启动、监控迁移，带中英文切换。
- 🔎 **迁移前兼容性评估（assess）** 与 **修复计划（remediate）** —— 迁移前先扫出 Firestore 不兼容点（集合名/文档 `_id`/字段名/文档大小/索引预算等），并给出可自动应用的修复策略。
- ✅ **迁移后校验（verify）** —— 比对源/目标文档数量，可选按内容哈希比对。
- 📊 **控制平面** —— `/metrics`、健康检查、状态 API、实时进度/延迟看板、割接就绪信号（cutover readiness）。

> ℹ️ 原始的 MongoDB→MongoDB 通用复制能力、三种复制方法（changestream / oplog / oplog-legacy）的详细前置条件，见仓库根目录的 [`README.md`](README.md)（英文）。本文件聚焦「怎么装、怎么配、怎么跑」以及 Firestore 目标的要点。

---

## 目录

1. [环境要求](#1-环境要求)
2. [快速开始](#2-快速开始)
3. [配置文件](#3-配置文件)
4. [运行方式一：浏览器控制台（推荐）](#4-运行方式一浏览器控制台推荐)
5. [运行方式二：命令行 CLI](#5-运行方式二命令行-cli)
6. [标准迁移流程：评估 → 修复 → 迁移 → 校验](#6-标准迁移流程评估--修复--迁移--校验)
7. [Firestore 目标要点](#7-firestore-目标要点)
8. [运行时产生的文件](#8-运行时产生的文件)
9. [常见问题排查](#9-常见问题排查)

---

## 1. 环境要求

- **Go 1.21 或更高**（本仓库在 Go 1.25 上开发/验证）。
- 可访问的 **源 MongoDB**（须是副本集，才能做增量复制/oplog 读取）。
- **目标**：另一套 MongoDB，或 **Firestore（MongoDB 兼容）** 实例。
- Linux / macOS（脚本为 bash）。

检查 Go：

```bash
go version
```

---

## 2. 快速开始

```bash
# 1) 克隆/下载本仓库后，进入目录
cd mongo-to-firestore

# 2) 编译（产物为当前目录下的 ./migrate，已被 .gitignore 忽略）
go build -o migrate ./cmd/migrate

# 3) 准备配置：复制示例模板并填入你自己的连接串
cp mongodb_full_migration_config.example.json mongodb_full_migration_config.json
#   然后编辑 mongodb_full_migration_config.json，替换所有 <占位符>

# 4a) 方式一：启动浏览器控制台（推荐，零配置也能用，表单里直接配）
./migrate -mode console -metrics-addr :9090
#   浏览器打开 http://localhost:9090/

# 4b) 方式二：直接命令行跑一次全量+增量迁移
./migrate -mode live -config mongodb_full_migration_config.json
```

> ⚠️ **不要把填了真实凭据的 `mongodb_full_migration_config.json` 提交进 git。** 仓库已在 `.gitignore` 里忽略该文件名；只提交 `*.example.json` 模板。

---

## 3. 配置文件

配置是一个 JSON 文件，核心是 `databasePairs` 数组（可配置多组源库→目标库同时迁移）。仓库提供三个示例模板：

| 模板文件 | 用途 |
|---|---|
| `mongodb_full_migration_config.example.json` | **多库对 MongoDB → Firestore** 全量+增量，含调优参数与重试配置（最常用，从这个改） |
| `sample_config.json` | 最简单的单库对示例（含 `indexOnly` 仅同步索引等选项） |
| `mongodb_replication_config.json` | MongoDB→MongoDB、按集合筛选 + `upsertMode` 的示例 |

### 3.1 最小结构

```json
{
  "databasePairs": [
    {
      "source": {
        "connectionString": "mongodb://<USER>:<PASSWORD>@<HOST>:27017/?authSource=admin&replicaSet=<RS_NAME>",
        "database": "source_db",
        "replicationMethod": "oplog-legacy"
      },
      "target": {
        "connectionString": "mongodb://<USER>:<PASSWORD>@<UID>.<LOCATION>.firestore.goog:443/<DB_ID>?loadBalanced=true&tls=true&authMechanism=SCRAM-SHA-256&retryWrites=false",
        "database": "<DB_ID>",
        "syncAllIndexes": true
      }
    }
  ]
}
```

### 3.2 关键字段

**source（源）**
- `connectionString`：源 MongoDB 连接串（增量复制要求源为副本集）。
- `database`：源数据库名。
- `replicationMethod`：复制方法，取值：
  - `"changestream"`（默认）—— MongoDB 3.6+，用 change stream。
  - `"oplog"` —— 直接读 oplog，适配较老版本（现代 wire protocol）。
  - `"oplog-legacy"` —— 兼容 MongoDB 3.0/3.2/3.4 等老版本。
  - 三者前置条件详见 [`README.md`](README.md)。

**target（目标）**
- `connectionString`：目标连接串（Firestore 端点格式见 [第 7 节](#7-firestore-目标要点)）。
- `database`：目标数据库名（Firestore 下即数据库 ID）。
- `syncAllIndexes`：是否把源上的二级索引（除 `_id_`）也同步过去。
- `indexOnly`：只同步索引、跳过数据（做补索引时用）。
- `upsertMode`：该目标库所有集合默认用 upsert 写入。
- `collections`：只迁移指定集合（数组，`sourceCollection`/`targetCollection`）。留空=整库迁移。
- `collectionTuning`：**按集合**覆盖分区并行读参数（用于「文档少但单条很大」的拖尾集合，评估器会自动推荐）。

**全局调优（节选，详见 `pkg/config/config.go` 注释）**
- 全量：`initialMigrationWorkers`（每集合写 worker 数）、`concurrentCollections`（并发几个集合）、`initialReadBatchSize`/`initialWriteBatchSize`。
- 增量：`incrementalWorkerCount`（变更应用 worker）、`incrementalReadBatchSize`。
- 大集合并行读：`parallelReadsEnabled`、`maxReadPartitions`、`minDocsPerPartition`、`minDocsForParallelReads`、`workersPerPartition`。
- 重试：`retryConfig`（`maxRetries` / `enableBatchSplitting` / `convertInvalidIds` 把非法 `_id` 类型转成字符串 等）。

> 💡 不用手动记这些值 —— **控制台的评估页会根据机器 CPU 和数据量自动推荐一套调优参数并预填表单**，也会为拖尾集合推荐 `collectionTuning`。

---

## 4. 运行方式一：浏览器控制台（推荐）

控制台无需预先准备配置文件，直接在网页表单里填连接串、评估、启动。

### 4.1 直接启动

```bash
go build -o migrate ./cmd/migrate
./migrate -mode console -metrics-addr :9090
# 打开 http://localhost:9090/
```

### 4.2 用看门狗脚本启动（长时间迁移推荐）

`run-console.sh` 是一个守护脚本：控制台进程若因任何原因退出，会在 ~2 秒内自动拉起，避免网页卡在「Failed to fetch」。

```bash
go build -o migrate ./cmd/migrate
./run-console.sh :9090          # 默认用 ./migrate；日志写 /tmp/console.log
# 自定义二进制路径：MIGRATE_BIN=/path/to/migrate ./run-console.sh :9090
```

重新编译后想让控制台加载新二进制，用**安全重启**脚本（它会拒绝在有任务运行时重启，避免丢任务）：

```bash
./restart-console.sh            # 有任务在跑会拒绝
./restart-console.sh --force    # 明确要丢弃当前任务再重启
```

> ⚠️ **重要：** 迁移任务的运行态存在进程内存里。**重启/杀进程会丢掉正在跑的任务**（需要从表单重新发起）。计划内的暂停请用界面上的「暂停」按钮，不要杀进程。

### 4.3 控制台功能

- **配置连接**：填源/目标连接串；`/api/detect` 自动探测源版本、库/集合清单，帮你选合适的复制方法。
- **Firestore 向导**：可列出/创建 Firestore 数据库与位置（`/api/firestore/*`）。
- **评估（assess）**：一键扫兼容性问题，给出发现明细、修复建议、以及推荐的调优参数（见第 6 节）。
- **启动/控制**：发起 migrate/live 任务；运行中可**暂停 / 恢复 / 停止**。
- **实时监控**：每集合进度、吞吐、复制延迟（lag）、**割接就绪信号**（延迟达标 + 无失败写 + 无死信，稳定若干周期后显示「就绪」）。
- **死信队列（DLQ）**：查看写失败的文档。
- **校验（verify）**：迁移后比对数量。
- **中英文切换**：右上角语言开关，界面与动态内容（评估发现、修复说明、调优理由等）都会跟随切换。

---

## 5. 运行方式二：命令行 CLI

```bash
./migrate [options]
```

主要参数（完整见 `./migrate -help`）：

| 参数 | 说明 |
|---|---|
| `-config <file>` | 配置文件路径（默认 `mongodb_replication_config.json`） |
| `-mode <mode>` | 运行模式，见下表（默认 `migrate`） |
| `-log-level` | `debug` / `info` / `warn` / `error`（默认 `info`） |
| `-log-file <file>` | 同时输出到文件 |
| `-metrics-addr :9090` | 开启控制平面（`/metrics`、健康检查、状态 API、看板 UI）。console 模式下即监听地址 |
| `-dry-run` | 演练：不写目标。全量阶段会输出分区建议；增量阶段打印读取延迟统计 |
| `-live-start-timestamp` | 仅 `live-only`：从指定时间点（Unix 秒或 RFC3339）开始增量 |
| `-verify-hash` | 仅 `verify`：除数量外再比对文档内容哈希 |
| `-id-map <file>` | 仅 `verify`：用 id-mapping JSONL 对齐被转换过的 `_id` |
| `-dlq-resync-from-source` | 仅 `retry-dlq`：按 `_id` 从**源**重新读取失败文档（拾取源端已修复），而非重放 DLQ 快照 |

**模式（`-mode`）：**

| 模式 | 作用 |
|---|---|
| `migrate` | 一次性全量迁移，完成即退出 |
| `live` | 全量迁移 + 随后转入实时增量复制，持续运行 |
| `live-only` | 只做实时增量（跳过全量），从上次 resume token 或指定时间点开始 |
| `retry-dlq` | 重新处理死信队列里的失败文档 |
| `assess` | 迁移前兼容性评估；有硬阻断问题时以非零退出码退出 |
| `verify` | 迁移后校验数量（可选内容哈希）；有差异时非零退出 |
| `wizard` | 交互式命令行生成配置文件 |
| `console` | 启动浏览器控制台 |

**示例：**

```bash
./migrate -mode wizard -config my_config.json          # 交互式生成配置
./migrate -mode assess -config my_config.json          # 迁移前评估
./migrate -mode live  -config my_config.json -log-file migration.log
./migrate -mode verify -config my_config.json -verify-hash
./migrate -mode retry-dlq -config my_config.json       # 重试死信
```

---

## 6. 标准迁移流程：评估 → 修复 → 迁移 → 校验

1. **评估（assess）** —— 扫描源数据里对 Firestore 不兼容的点：
   - 集合名（长度/保留字/含 `$`/`system.` 前缀）
   - 文档 `_id`（长度/保留值/需类型转换）
   - 文档大小、嵌套深度、字段名长度/保留字、字段值大小
   - 索引预算、数据库预算
   ```bash
   ./migrate -mode assess -config my_config.json
   ```
   或在控制台点「评估」。评估会读取已保存的修复计划（`remediation-plan.json`），使 CLI 与控制台结果一致。

2. **修复（remediate）** —— 对每类问题给出默认修复策略（重命名集合、重写 `_id`、序列化超深嵌套、清洗字段名、切分超大值等），可保存成修复计划在迁移时自动应用。控制台里逐条选择即可。

3. **迁移（migrate / live）** —— 用评估推荐（或你调整过）的参数启动全量 + 增量。

4. **校验（verify）** —— 迁移后比对源/目标数量，必要时 `-verify-hash` 比对内容；若迁移中转换过 `_id`，用 `-id-map` 对齐。
   ```bash
   ./migrate -mode verify -config my_config.json
   ```

---

## 7. Firestore 目标要点

**连接串格式：**

```
mongodb://<USER>:<PASSWORD>@<UID>.<LOCATION>.firestore.goog:443/<DB_ID>?loadBalanced=true&tls=true&authMechanism=SCRAM-SHA-256&retryWrites=false
```

- `<UID>.<LOCATION>.firestore.goog` 是 Firestore MongoDB 兼容端点主机名（`<LOCATION>` 如 `asia-northeast1`）。
- 端口固定 `443`，必须带 `tls=true`、`loadBalanced=true`。
- **`retryWrites=false` 必填**（Firestore 兼容层不支持可重试写）。
- `authMechanism=SCRAM-SHA-256`。
- `<DB_ID>` 既是路径里的数据库，也应填到 `target.database`。

**注意事项：**
- Firestore 有集合名/文档大小/字段名/索引等一系列限制 —— **务必先跑评估**。
- `retryConfig.convertInvalidIds: true` 可把非法 `_id` 类型自动转成字符串（并记录到 id-mapping，供 verify 对齐）。
- 💰 **Firestore 是计费资源**：测试完记得删除测试用的数据库/实例，避免持续计费。

---

## 8. 运行时产生的文件

工具运行时会在工作目录生成一批状态/日志文件。这些都**不应提交进 git**，`.gitignore` 已全部忽略：

| 文件 | 含义 | 能否删 |
|---|---|---|
| `resumeToken-*.json`（含 `.1`/`.2`… 备份） | 增量复制断点（resume token）| 任务不再续跑时可删 |
| `oplogTimestamp-*.json[.backup]` | oplog 复制的位点 | 同上 |
| `initialMigrationState-*.json` | 全量迁移进度状态 | 全量完成后可删 |
| `id-mapping.jsonl` | 被转换过的 `_id` 映射（供 verify）| 校验完可删 |
| `dlq-*.jsonl` | 死信队列（写失败的文档）| 处理完/确认放弃后可删 |
| `remediation-plan.json`（+`.bak-*`）| 评估修复计划 | 迁移完成后可删 |
| `remediation-audit.jsonl` | 修复动作审计日志 | 归档后可删 |
| `*.log` / `console-*.log` | 运行日志 | 可删 |
| `*.pid`（如 `/tmp/console.pid`）| 看门狗记录的进程号 | 由脚本管理 |
| `migrate` | 编译产物 | 可删（重新 `go build` 即可）|

> ⚠️ **不要删正在运行任务的状态文件**（resumeToken / initialMigrationState / dlq 等）—— 会破坏断点续传。删之前先确认对应任务已停止。

---

## 9. 常见问题排查

- **控制台网页 “Failed to fetch”**：控制台进程可能退出了。用 `./run-console.sh` 守护启动可自动拉起；或检查 `/tmp/console.log`。
- **`restart-console.sh` 拒绝重启**：说明有任务在跑（保护机制，避免丢任务）。想暂停用界面「暂停」；确实要丢弃任务再重启加 `--force`。
- **写 Firestore 报错 / 大量重试**：确认连接串带了 `retryWrites=false&tls=true&loadBalanced=true`；开启 `retryConfig.enableBatchSplitting` 应对争用；`convertInvalidIds` 处理非法 `_id`。
- **评估报硬阻断（B 类）问题**：先在修复流程里处理（重命名/转换等）再迁移；`assess` 模式遇硬阻断会以退出码 1 退出。
- **老版本 MongoDB（3.0/3.2/3.4）连不上做增量**：把 `replicationMethod` 设为 `oplog-legacy`。
- **文档少但单条巨大的集合迁移很慢**：评估会推荐 `collectionTuning` 强制该集合分区并行读；也可手动在目标配置里加。
- **打开文件数不足**：工具会尝试把 `ulimit -n` 提到 65536；若系统硬上限更低会取系统上限并告警。

---

## 相关文档

- [`README.md`](README.md) —— 原始工具的完整说明（复制方法前置条件、索引策略等，英文）。
- [`README_STATS.md`](README_STATS.md) —— 统计/指标说明。
- [`DESIGN.md`](DESIGN.md) —— 设计文档。
- [`CONSOLE_UI_DESIGN.md`](CONSOLE_UI_DESIGN.md) —— 控制台 UI 设计契约。
- [`docs/`](docs/) —— 补充文档（legacy 全量、索引策略）。
